const TheLogList = (function () {
    "use strict";

    var template = function () {
        return `
        `;
    };

    var module = Vue.component('the-log-list', {
        template: template(),
        data: function () {
            return ({
            });
        }
    });

    return (module);
})();