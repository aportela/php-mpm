const TheSearchTemplateList = (function () {
    "use strict";

    var template = function () {
        return `
        `;
    };

    var module = Vue.component('the-search-template-list', {
        template: template(),
        data: function () {
            return ({
            });
        }
    });

    return (module);
})();