const TheUserList = (function () {
    "use strict";

    var template = function () {
        return `
        `;
    };

    var module = Vue.component('the-user-list', {
        template: template(),
        data: function () {
            return ({
            });
        }
    });

    return (module);
})();