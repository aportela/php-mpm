const TheGroupList = (function () {
    "use strict";

    var template = function () {
        return `
        `;
    };

    var module = Vue.component('the-group-list', {
        template: template(),
        data: function () {
            return ({
            });
        }
    });

    return (module);
})();