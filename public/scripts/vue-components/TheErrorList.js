const TheErrorList = (function () {
    "use strict";

    var template = function () {
        return `
        `;
    };

    var module = Vue.component('the-error-list', {
        template: template(),
        data: function () {
            return ({
            });
        }
    });

    return (module);
})();