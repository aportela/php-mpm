const TheAttributeList = (function () {
    "use strict";

    var template = function () {
        return `
        `;
    };

    var module = Vue.component('the-attribute-list', {
        template: template(),
        data: function () {
            return ({
            });
        }
    });

    return (module);
})();